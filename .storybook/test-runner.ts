import type { TestRunnerConfig } from '@storybook/test-runner';
// Optional: tweak test runner (timeouts, viewport, etc.).
const config: TestRunnerConfig = {
  // Increase if your stories take longer to render
  // timeout: 8000,
};
export default config;
